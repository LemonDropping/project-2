{{#if loggedIn}}
    <main>
        <h2 id="secondTitle">Platforms</h2>

        <section id="platformCard">

        </section>
    </main>
{{else}}
<a href="/login">You must log in first!</a>
{{/if}}